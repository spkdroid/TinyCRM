# TinyCRM Docker Configuration
# Copy this to .env and modify as needed

# Database Configuration
DB_ROOT_PASSWORD=secure-root-password-2024
DB_NAME=support_module
DB_USER=app_user
DB_PASSWORD=secure-app-password-2024
DB_PORT=3306

# Backend Configuration
BACKEND_PORT=8080
SPRING_PROFILE=docker
LOG_LEVEL=INFO

# Frontend Configuration
FRONTEND_PORT=3000

# Environment
NODE_ENV=production